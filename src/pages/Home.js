import React from "react";
import { Section, Header } from "./home.styling";

const Home = () => {
    return (
        <Section>
            <Header>home</Header>
        </Section>
    );
};

export default Home;
